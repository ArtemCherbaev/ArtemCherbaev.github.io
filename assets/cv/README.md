Drop the CV PDF here as `Artem-Cherbaev-CV.pdf`.

The header button, the contact list and the CV page all link to that exact
filename, so nothing else needs editing once the file is in place. Until it is,
those links 404, which the smoke suite reports rather than hides.
